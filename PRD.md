版本：V1.0 Final
平台：Web (Responsive) - 响应式网页
核心目标：验证“微课学习 + AI 知识生长 + 间隔复习”的学习闭环体验。
数据策略：Mock Data First (纯前端模拟，无后端)。

---
1. 用户旅程架构 (User Journey Architecture)
阶段一：承诺与预期 (Onboarding)
1. 用户进入 Landing Page，被问及：“你每天能投入多少碎片时间？”
2. 用户拖动滑块（15min - 60min）。
3. 系统根据算法（总课时 / 每日时长）实时计算并显示：“预计 X月X日 达成 AI 产品经理 Offer 标准”。
4. 用户确认，产生心理契约，进入首页。
阶段二：全局导航 (Mission Control)
1. 用户看到四大核心模块（基础、拆解、实操、面经）的进度概览。
2. 用户选择一个模块（如“基础知识”）进入学习流。
阶段三：知识输入与生长 (Input & Growth)
1. 沉浸学习：像刷小红书一样，竖滑切换知识点，横滑查看详情。
2. 遇到不懂：点击“AI 提问”一个悬浮小球，弹出小窗口，和ai对话获得解释。
3. 知识捕获：在ai对话窗口对话完毕后，可以在里面点击“Pin (钉住)”，将 用户与AI 的问答作为新的一页挂载到当前知识点末尾。
4. 即时反馈：进度条多了一颗金星，用户横滑即可看到刚刚生成的“私有笔记”。
阶段四：间隔复习 (The SRS Loop)
1. 第二天，系统推送“今日需复习 5 个知识点”。
2. 用户进入复习模式，看到的内容包含：官方正文 + 官方闪卡 + 自己之前挂载的笔记。
3. 用户根据掌握程度选择状态（忘了/模糊/熟练），系统决定该知识点下次出现的时间。

---
2. UI 结构与交互细节 (UI/UX Specifications)
应用采用 底部导航栏 (Bottom Navigation) 结构，包含三个 Tab。
📱 Tab 1: 首页 (Mission Control)
2.1 顶部仪表盘 (Dashboard)
- 倒计时卡片：显示“距离 Offer 还有 XX 天”（基于 Onboarding 的设置）。
- 今日圆环：显示“今日已学 X 分钟 / 目标 Y 分钟”。
2.2 核心模块入口 (Grid Layout)
展示四个大卡片，点击分别进入不同的 Feed 或 页面：
graph TD
    %% ==========================================
    %% 0. 入口与引导 (Onboarding)
    %% ==========================================
    Start(用户访问 Web App) --> OnboardingPage[📄 引导页: Onboarding]
    OnboardingPage -->|1. 拖动滑块| ActionSetTime[⚙️ 设置每日时长]
    ActionSetTime -->|2. 实时反馈| PromiseDisplay[👁️ 显示: 预计拿Offer日期]
    PromiseDisplay -->|点击开始| MainScaffold[📱 全局导航框架<br/>(BottomNav / SideNav)]

    %% ==========================================
    %% 1. 核心导航 (Main Navigation)
    %% ==========================================
    MainScaffold -->|Tab 1| HomePage[🏠 首页: Mission Control]
    MainScaffold -->|Tab 2| VaultPage[🧠 复习: My Vault]
    MainScaffold -->|Tab 3| ProfilePage[👤 我的: Profile]

    %% ==========================================
    %% 2. 首页分支 (Home Tab Flow)
    %% ==========================================
    subgraph Home_Flow [首页功能区]
        HomePage -->|顶部| Dashboard[📊 仪表盘: 倒计时 + 今日进度]
        HomePage -->|中部| ModuleGrid[🗂️ 四大模块入口]
        
        ModuleGrid -->|模块 A/B: 基础 & 拆解| FeedPage[📱 学习流: Feed Page]
        ModuleGrid -->|模块 C: 实操| LabPage[🛠️ 实操页: Hands-on Lab]
        ModuleGrid -->|模块 D: 面经| WarRoom[⚔️ 面经军火库: War Room]
    end

    %% 2.1 学习流细节 (The Feed Loop)
    subgraph Feed_Detail [通用学习流交互]
        FeedPage -->|竖滑| NextCard[切换知识点]
        FeedPage -->|横滑| CardDetail[P2/P3: 详情页]
        
        FeedPage -->|点击提问| ChatSheet[🤖 弹窗: Ask AI Sheet]
        ChatSheet -->|Ask & Reply| PinAction[📌 动作: Pin to Card]
        PinAction -->|生成新页面| UserNotePage[📝 页面: 私有笔记页]
        UserNotePage -.->|挂载| FeedPage
    end

    %% 2.2 实操模块细节 (Module C)
    subgraph Lab_Detail [实操模块交互]
        LabPage --Mobile--> MobileLabView[纯阅读流 + 电脑端引导]
        LabPage --Desktop--> WebLabView[阅读流 + 常驻 AI 侧边栏]
        WebLabView -->|高频提问| LabChat[💬 实操伴随问答]
    end

    %% 2.3 面经模块细节 (Module D)
    subgraph WarRoom_Detail [面经军火库交互]
        WarRoom --Mobile--> MobileWarView[浏览模板 + 上传简历]
        WarRoom --Desktop--> DesktopWarView[🖥️ 三栏工作台]
        
        DesktopWarView -->|左栏| DocTree[目录树]
        DesktopWarView -->|中栏| EditorArea[阅读/克隆区]
        DesktopWarView -->|右栏| MyDocs[我的文档库]
        
        EditorArea -->|AI Rewrite| CloneAction[✨ 生成我的回答]
        CloneAction --> MyDocs
        
        WarRoom -->|点击模拟| MockInterview[💬 模拟面试 Chat]
        MockInterview -->|随机抽查| MyDocs
    end

    %% ==========================================
    %% 3. 复习分支 (Vault Tab Flow)
    %% ==========================================
    subgraph Review_Flow [SRS 复习闭环]
        VaultPage -->|判断 isDue?| ReviewList[📋 待复习列表]
        ReviewList -->|点击卡片| SRSView[🔁 复习模式页面]
        
        SRSView -->|包含| Content[官方内容 + 私有笔记]
        SRSView -->|底部| SRSButtons[评级按钮: 忘了/模糊/熟练]
        SRSButtons -->|更新算法| NextReview[安排下次复习]
        NextReview -->|自动跳转| SRSView
    end

    %% ==========================================
    %% 4. 个人中心 (Profile Flow)
    %% ==========================================
    ProfilePage -->|修改| ResetSlider[重设每日时长]
    ResetSlider -.->|更新全局状态| Dashboard

    %% 样式定义
    style Start fill:#f9f,stroke:#333,stroke-width:2px
    style MainScaffold fill:#e1f5fe,stroke:#01579b
    style Feed_Detail fill:#fff3e0,stroke:#e65100,stroke-dasharray: 5 5
    style Review_Flow fill:#e8f5e9,stroke:#1b5e20
    style WarRoom_Detail fill:#f3e5f5,stroke:#4a148c
📦 模块A：AI 硬核基础 (Fundamentals)：
- 元数据：180 知识点 | 预计 30 小时。
- 行为：跳转 FeedPage (加载 Module A 数据)。
🔮 模块B：产品拆解与 Sense (Case Studies)：
- 元数据：50 案例 | 预计 10 小时。
- 行为：跳转 FeedPage (加载 Module B 数据)。
📦 模块 C：全栈实操 (Hands-on Lab) - 伴随式指引
此模块不提供内置的代码编辑器，而是作为“教程 + 问答伴侣”，指导用户在第三方工具（如 Figma/Coze）中操作。
核心逻辑
内容形式：Step-by-Step 的图文教程（Markdown 渲染）。
- 元数据：50 教程 | 预计 10 小时。
- 行为：跳转 LabPage (加载 Module C 数据)。
交互重点：“高频提问”。因为实操容易报错，用户需要频繁问 AI。
多端适配策略
手机端 (Mobile)：
  FeedPage (加载 Module C 数据)。但是，底部显示“建议电脑端打开以实操”的 Toast。
桌面端 (Web Desktop)：
  布局维持：依然保持阅读流样式
  视觉强化：
    Ask AI 按钮：从右下角的悬浮小球，变为右侧常驻的侧边栏（或者一个非常显眼的大按钮），方便用户一边看教程，一边随时把报错信息粘贴进去问 AI。
    窗口建议：进入时弹窗提示：“建议将本窗口吸附在屏幕左侧，将实操工具（Coze/Figma）放在右侧”。

---
⚔️ 模块 D：面经军火库 (War Room) - 克隆与模拟
这是一个独立的“高阶功能区”，拥有自己独立的资料库和复习入口，不混入通用的 SRS 算法。
核心流程 (The Workflow)
浏览模板：查看官方精选的“金标准回答”。
AI 克隆 (Clone)：点击“用我的经历重写”，与 AI 对话生成属于用户的版本。
独立复习 (Mock Interview)：基于用户已保存/已克隆的文档，AI 扮演面试官进行抽查。
多端适配策略
手机端 (Mobile)：
  功能限制：
    支持 查看 模板库。
    支持 上传 简历/JD（为电脑端操作做准备）。
    支持 进入“模拟面试”（文字版对答）。
  引导：点击“克隆/重写”按钮时，提示“涉及大量文本编辑，建议前往电脑端获得最佳体验”。
桌面端 (Web Desktop)：
  布局架构：三栏式“工作台”布局。
    左栏 (Library)：模板目录树 (Tree View)。可以点击一键克隆
    中栏 (Workspace)： “一键克隆”交互区 (Chat Interface)，文档编辑区，用户在此上传jd以及和有窗口和ai聊天，用户往里面填入ai生成的内容。
    右栏 (My Arsenal)：用户已生成/已保存的文档列表。
  特别入口：
    在界面右上方有一个显眼的 “开始模拟面试 (Start Mock Quiz)” 按钮。
    逻辑：点击后，AI 读取右栏（用户私有文档）的内容，随机抽取一个知识点提问：“针对你写的关于‘数据指标’的那个项目，如果指标下跌了你怎么分析？”

---
📚 Tab 2: 学习流页面 (The Feed Page)
此页面在点击模块后进入，隐藏底部导航栏。
2.3 浏览交互 (The Viewer)
- 双轴滑动：
  - 竖向 (Vertical)：切换不同的 FeedItem (知识点 ID: 101 -> 102)。
  - 横向 (Horizontal)：查看当前 ID 的详情页 (Page 1 -> Page 2 -> User Note Page)。
- 底部进度条：显示当前 ID 的阅读进度（如 ⚪⚪⚪）。如果用户 Pin 了笔记，变为 ⚪⚪⚪🟡。
2.4 右侧交互栏 (Action Bar)
悬浮在屏幕右下侧：
- ❤️ 点赞：微交互动画。
- 🤖 提问 (Ask AI) - [核心]：
  - 点击弹出半屏聊天窗口 (BottomSheet)。
  - 功能：输入问题 -> AI 回复 (Mock) -> 点击 "Pin to Card" 按钮。
  - Pin 的反馈：Toast 提示“笔记已添加”，窗口关闭，主界面自动滑到最后一页（即刚生成的那页）。

---
🧠 Tab 3: 复习与仓库 (Vault / Review)
2.5 待复习列表 (Due List)
- 只显示算法判断为 isDue == true 的知识点。
- 如果今日无任务，显示空状态插画。
2.6 复习模式 (SRS Mode)
- 进入某个知识点的详情页。
- 差异点：底部不显示进度条，而是显示 SRS 评估栏。
- 按钮逻辑：
  - [🔴 忘了] -> 设置 nextReview = 10分钟后。
  - [🟡 模糊] -> 设置 nextReview = 1天后。
  - [🟢 熟练] -> 设置 nextReview = 3天后。
- 点击任意按钮后，当前卡片飞出，自动加载下一张。

---
⚙️ Tab 4: 个人中心 (Profile)
- 每日设置：包含一个 Slider，允许用户重新调整“每日投入时长”，调整后首页倒计时需更新。
- 付费状态：Free / Pro 标识。

---
3. 技术实现细节 (Technical Implementation)
3.1 技术栈 (Tech Stack)
- Framework: Flutter (Target: Web).
- State Management: flutter_riverpod (必须使用，用于处理 FeedItem 的动态增删改)。
- Router: go_router (处理 /home, /feed/:id 等路径)。
- Markdown: flutter_markdown (用于渲染知识点正文)。
3.2 数据结构定义 (Mock Data Schema)
请在 mock_data.dart 中严格实现以下结构，以支持“挂载”逻辑：
Dart
// 页面基类abstract class CardPageContent {
  final String type; // 'text', 'image', 'user_note'// ...
}

// 官方内容页class OfficialPage extends CardPageContent {
  final String markdownContent;
  OfficialPage(this.markdownContent) : super(type: 'text');
}

// 用户私有笔记页 (动态生成)class UserNotePage extends CardPageContent {
  final String question;
  final String answer;
  final DateTime createdAt;
  UserNotePage({required this.question, required this.answer, required this.createdAt}) : super(type: 'user_note');
}

// 核心知识点容器class FeedItem {
  final String id;
  final String moduleId; // 'A', 'B', 'C', 'D'final String title;
  
  // 页面列表：初始只有 officialPages，运行时会 append userNotesfinal List<CardPageContent> pages; 
  
  // SRS 算法数据DateTime? nextReviewTime;
  int intervalDays;

  FeedItem({
    required this.id, 
    required this.moduleId,
    required this.title,
    required this.pages,
    this.nextReviewTime,
    this.intervalDays = 0,
  });
}
3.3 关键逻辑伪代码 (Critical Logic)
Logic 1: 挂载笔记 (The Pin Action)
Dart
// 在 Riverpod 的 Provider 中void pinNoteToItem(String itemId, String question, String answer) {
  state = [
    for (final item in state)
      if (item.id == itemId)
        item.copyWith(
          pages: [...item.pages, UserNotePage(question, answer, now)]
        )
      else
        item
  ];
}
Logic 2: 承诺算法 (The Promise Math)
Dart
String calculateOfferDate(int dailyMinutes) {
  const totalCourseMinutes = 6000; // 假设总时长int daysNeeded = (totalCourseMinutes / dailyMinutes).ceil();
  return formatDate(DateTime.now().add(Duration(days: daysNeeded)));
}


